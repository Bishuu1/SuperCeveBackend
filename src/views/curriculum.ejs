<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Lista</title>
</head>
<body>

    
    <h1>Lista de Curriculum</h1>
        
        <ul>
        <% for(var i =0; i< items.length;i++ ) {%>
            <li><%=items[i].name%></li>
            <% }%>
        </ul>
        <h3>Crea un curriculum</h3>
        <form action="/New-CV" method="POST">
            <input type="text" name="newcv" id="newcv">
            <button type="button" name="button">
                Agregue Nuevo CV
            </button>
        </form>

        <nav>
                <ul>
                    <li><a href="/">Volver al Inicio</a></li>
                </ul>
            </nav>
            <script>
            const txtnewcv = document.getElementById('newcv');
            txtnewcv.focus();
            </script>
</body>
</html>
